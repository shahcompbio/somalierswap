{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/shahcompbio/somalierswap/main/assets/schema_input.json",
    "title": "shahcompbio/somalierswap pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "subject": {
                "type": "string",
                "description": "Subject/patient identifier, used internally to perform sanity check when processing multiple groups",
                "pattern": "^\\S+$",
                "errorMessage": "subject must be provided and cannot contain spaces",
                "meta": ["subject"]
            },
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "bam": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^([\\S\\s]*\\/)?[^\\s\\/]+\\.bam$",
                "errorMessage": "bam file must be provided, cannot contain spaces and must have extension '.bam'"
            },
            "bai": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^([\\S\\s]*\\/)?[^\\s\\/]+\\.bai$",
                "errorMessage": "bai file must be provided, cannot contain spaces and must have extension '.bai'"
            }
        },
        "required": ["sample", "bam", "bai"]
    }
}
